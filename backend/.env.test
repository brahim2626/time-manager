# ================================================
# .ENV.TEST — Variables pour les tests
# Base de données SÉPARÉE pour ne pas polluer les données réelles
# ================================================

NODE_ENV=test
PORT=4001

# Base de données de TEST (différente de dev !)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=time_manager_dev
DB_USER=tm_user
DB_PASSWORD=tm_password_dev_2024

JWT_SECRET=test_secret_key_for_jest
JWT_EXPIRES_IN=1h
BCRYPT_ROUNDS=1
# ↑ BCRYPT_ROUNDS=1 en test : le hashage est ultra-rapide
# En prod c'est 10, mais en test on veut des tests rapides